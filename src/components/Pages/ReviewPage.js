const ReviewPage = () => {
  return (
    <div style={{ textAlign: "center" }}>
      TO COMPLETE ... (all courses with hours and credits, total hours total
      credits)
    </div>
  );
};

export default ReviewPage;
